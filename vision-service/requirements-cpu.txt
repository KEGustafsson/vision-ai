# CPU detection backend: real YOLO11 inference on CPU (mode: cpu, backend:
# torch-cpu). Pulls in torch via ultralytics, so this is a large install —
# needed only for CPU-based detections (non-Jetson). Mock mode does NOT need it.
-r requirements.txt
ultralytics>=8.3,<9   # 8.3+ ships YOLO11; cap to the 8.x API used by the detector/export code
